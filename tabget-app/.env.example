# Backend API base URL
# Dev: "/api" (uses Vite proxy → http://localhost:3000)
# Prod: absolute URL, e.g. "https://api.tabget.example.com"
VITE_API_BASE=/api

# Mock 모드 — true 시 backend 없이 localStorage 기반 동작 (GH Pages 배포용)
# Dev 모드 기본값은 .env.development 에서 true로 설정
VITE_USE_MOCK=false
